interface:
  display_name: "Generate Report"
  short_description: "Render performance Git reports from JSON"
  default_prompt: "Use $generate-report to turn git-search JSON into a performance Markdown report."
policy:
  allow_implicit_invocation: true
